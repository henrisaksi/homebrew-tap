# homebrew-tap

Homebrew tap for personal macOS utilities and CLIs.

## Available Formulae

### `reminders`
A fast CLI for Apple Reminders on macOS with support for tags, sections, and location-based triggers.

#### Installation
```bash
brew install henrisaksi/tap/reminders
```
